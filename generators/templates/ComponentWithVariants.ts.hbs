import tw, { styled, css, theme, TwStyle } from 'twin.macro';

export interface {{pascalCase name}}Props {
  /**
   * The visual style of the {{pascalCase name}}
   * @default "primary"
   */
  variant?: 'primary' | 'secondary';
  /**
   * The size of the Button {{pascalCase name}}
   * @default "md"
   */
  size?: 'xs' | 'sm' | 'md' | 'lg';
}

const primaryVariants = {
  // Named class sets
  primary: tw` `,
  secondary: tw` `,
};

const sizeVariants = {
  // Named class sets
  sm: tw` `,
  md: tw` `,
  lg: tw` `,
};

export const {{pascalCase name}}: FC<{{pascalCase name}}Props> = styled.div(({ variant, size }) => [
    // Base Styles
    tw` `,

    // Dynamic Styles
    primaryVariants[variant],
    sizeVariants[size],
  ]
);

{{pascalCase name}}.defaultProps = {
  variant: 'primary',
  size: 'md',
};
